---
phase: 03-mdx-components
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/mdx/Img.astro
  - src/components/mdx/Link.astro
  - src/components/mdx/Table.astro
  - src/components/mdx/Blockquote.astro
  - src/pages/blogs/[slug].astro
  - src/styles/globals.css
autonomous: false
requirements:
  - MDX-05
  - MDX-06

must_haves:
  truths:
    - "Markdown images render with lazy loading and responsive width"
    - "Post 9 explicit Img JSX import continues to work without modification"
    - "Links in MDX open in new tab with underline styling"
    - "Tables render with borders and overflow scrolling on narrow viewports"
    - "Blockquotes render with left border accent"
    - "All 9+ MDX posts render without import errors or broken components"
  artifacts:
    - path: "src/components/mdx/Img.astro"
      provides: "Image override for markdown images"
      contains: "loading"
    - path: "src/components/mdx/Link.astro"
      provides: "Link override for markdown links"
      contains: "target"
    - path: "src/components/mdx/Table.astro"
      provides: "Table with overflow wrapper"
      contains: "overflow"
    - path: "src/components/mdx/Blockquote.astro"
      provides: "Styled blockquote"
      contains: "border-l"
    - path: "src/pages/blogs/[slug].astro"
      provides: "MDX components wired via Content components prop"
      contains: "components="
  key_links:
    - from: "src/pages/blogs/[slug].astro"
      to: "src/components/mdx/*.astro"
      via: "components prop on Content"
      pattern: "components="
    - from: "src/components/ui/img.tsx"
      to: "blogs/9-*.mdx"
      via: "import Img from components/ui/img"
      pattern: "export function Img"
---

<objective>
Create the remaining simple MDX component overrides (Img, Link, Table, Blockquote), wire ALL components into the blog post page via the `components` prop on Content, add prose typography CSS, and verify all 9 posts render correctly.

Purpose: Completes the MDX component layer by adding simple element overrides and connecting everything to the blog post rendering pipeline.
Output: 4 new component files, updated [slug].astro with components prop, prose typography CSS, verified rendering of all posts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mdx-components/03-RESEARCH.md
@.planning/phases/03-mdx-components/03-01-SUMMARY.md
@src/pages/blogs/[slug].astro
@src/components/ui/img.tsx

<interfaces>
<!-- From Plan 01 (created components): -->
<!-- src/components/mdx/H2.astro through H6.astro with anchor links -->
<!-- src/components/mdx/CodeBlock.astro with copy button -->
<!-- src/components/mdx/InlineCode.astro (standalone, NOT in components map) -->

<!-- Current [slug].astro: uses Content with no components prop -->

<!-- Old mdx-components.tsx element styling reference: -->
<!-- img: span.my-6.block wrapping Image with class="max-w-full w-full m-auto h-auto border-2" -->
<!-- a: class="font-medium text-primary underline underline-offset-4 hover:text-primary/80 transition-colors" target="_blank" -->
<!-- blockquote: class="mt-6 border-l-2 border-primary pl-6 italic text-muted-foreground" -->
<!-- table wrapper: class="my-6 w-full overflow-y-auto" -->
<!-- table: class="w-full border-collapse border border-border" -->
<!-- thead: class="bg-muted" -->
<!-- tr: class="border-b border-border transition-colors hover:bg-muted/50" -->
<!-- th: class="border border-border px-4 py-2 text-left font-bold" -->
<!-- td: class="border border-border px-4 py-2 text-left" -->
<!-- p: class="leading-7 [&:not(:first-child)]:mt-6 text-muted-foreground" -->
<!-- ul: class="my-6 ml-6 list-disc [&>li]:mt-2 text-muted-foreground" -->
<!-- ol: class="my-6 ml-6 list-decimal [&>li]:mt-2 text-muted-foreground" -->
<!-- li: class="leading-7" -->
<!-- hr: class="my-8 border-border" -->
<!-- strong: class="font-bold text-foreground" -->

<!-- src/components/ui/img.tsx: export function Img(props: { src: string; alt: string }) -->
<!-- Used by blog post 9 via: import { Img } from '@/components/ui/img' -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create simple MDX components and wire into [slug].astro</name>
  <files>
    src/components/mdx/Img.astro,
    src/components/mdx/Link.astro,
    src/components/mdx/Table.astro,
    src/components/mdx/Blockquote.astro,
    src/pages/blogs/[slug].astro,
    src/styles/globals.css
  </files>
  <read_first>
    src/pages/blogs/[slug].astro,
    src/components/ui/img.tsx,
    src/components/mdx/H2.astro,
    src/components/mdx/CodeBlock.astro,
    src/styles/globals.css
  </read_first>
  <action>
**1. Create src/components/mdx/Img.astro** -- Override for markdown `![alt](src)` syntax.

Astro frontmatter section: destructure `const { src, alt, ...rest } = Astro.props;`

Template: A `span` with class `my-6 block` wrapping an `img` element with these attributes:
- `src={src}`
- `alt={alt || ''}`
- `loading="lazy"`
- `class="max-w-full w-full m-auto h-auto border-2"`
- `{...rest}` spread for any additional attributes

**2. Keep src/components/ui/img.tsx as-is** -- Post 9 uses `import { Img } from '@/components/ui/img'` as an explicit JSX import. This is a different code path from the `img` override in the components map. The existing stub already works. Do NOT modify or delete it.

**3. Create src/components/mdx/Link.astro** -- Override for markdown `[text](url)`.

Astro frontmatter section: `const { href, ...rest } = Astro.props;` and `const isExternal = href?.startsWith('http') || href?.startsWith('//');`

Template: An `a` element with:
- `href={href}`
- `class="font-medium text-primary underline underline-offset-4 hover:text-primary/80 transition-colors"`
- Conditionally add `target="_blank"` and `rel="noopener noreferrer"` only when `isExternal` is true. Use the Astro spread pattern: `{...(isExternal ? { target: '_blank', rel: 'noopener noreferrer' } : {})}`
- `{...rest}` spread
- `<slot />` as children

External links (starting with `http` or `//`) get target="_blank". Internal links (starting with `/` or `#`) stay in same tab. Fragment links (`#id`) for heading anchors must NOT open in new tab.

**4. Create src/components/mdx/Table.astro** -- Override for markdown tables (GFM via remark-gfm).

Astro frontmatter section: `const props = Astro.props;`

Template: A `div` with class `my-6 w-full overflow-x-auto` wrapping a `table` with:
- `class="w-full border-collapse border border-border"`
- `{...props}` spread
- `<slot />` as children

**5. Create src/components/mdx/Blockquote.astro** -- Override for markdown `>` blockquotes.

Astro frontmatter section: `const props = Astro.props;`

Template: A `blockquote` element with:
- `class="mt-6 border-l-2 border-primary pl-6 italic text-muted-foreground"`
- `{...props}` spread
- `<slot />` as children

**6. Update src/pages/blogs/[slug].astro** -- Import all MDX components and pass via `components` prop. Replace the entire file with these contents:

Astro frontmatter section with these imports:
- `import { getCollection, render } from 'astro:content';`
- `import BlogLayout from '@/layouts/BlogLayout.astro';`
- `import H2 from '@/components/mdx/H2.astro';`
- `import H3 from '@/components/mdx/H3.astro';`
- `import H4 from '@/components/mdx/H4.astro';`
- `import H5 from '@/components/mdx/H5.astro';`
- `import H6 from '@/components/mdx/H6.astro';`
- `import CodeBlock from '@/components/mdx/CodeBlock.astro';`
- `import Img from '@/components/mdx/Img.astro';`
- `import Link from '@/components/mdx/Link.astro';`
- `import Table from '@/components/mdx/Table.astro';`
- `import Blockquote from '@/components/mdx/Blockquote.astro';`

Then define the components map object `mdxComponents` with these exact entries:
- `h2: H2, h3: H3, h4: H4, h5: H5, h6: H6` (heading overrides)
- `pre: CodeBlock` (code block override -- NOT `code`)
- `img: Img` (image override)
- `a: Link` (link override)
- `table: Table` (table override)
- `blockquote: Blockquote` (blockquote override)

Keep the existing `getStaticPaths`, props destructuring, and `render(post)` call unchanged. Change the template to pass components: `<Content components={mdxComponents} />`

Key constraints:
- Do NOT map `h1` -- blog title is rendered by BlogLayout, not MDX
- Do NOT map `code` -- inline code styled via global CSS; mapping code interferes with Shiki's pre>code output
- Do NOT map `p`, `ul`, `ol`, `li`, `hr`, `strong`, `em` -- styled via global CSS below

**7. Add global prose typography CSS to src/styles/globals.css** -- Append these rules after the existing content (after the inline code rule added by Plan 01):

Table child styling:
```css
/* MDX table styling */
.prose thead { @apply bg-muted; }
.prose tr { @apply border-b border-border transition-colors hover:bg-muted/50; }
.prose th { @apply border border-border px-4 py-2 text-left font-bold; }
.prose td { @apply border border-border px-4 py-2 text-left; }
```

Prose typography (matching old mdx-components.tsx exactly):
```css
/* MDX prose typography */
.prose p { @apply leading-7 [&:not(:first-child)]:mt-6 text-muted-foreground; }
.prose ul { @apply my-6 ml-6 list-disc [&>li]:mt-2 text-muted-foreground; }
.prose ol { @apply my-6 ml-6 list-decimal [&>li]:mt-2 text-muted-foreground; }
.prose li { @apply leading-7; }
.prose hr { @apply my-8 border-border; }
.prose strong { @apply font-bold text-foreground; }
.prose em { @apply italic; }
.prose img { @apply my-6 max-w-full w-full m-auto h-auto; }
```

These global styles leverage the `.prose` class already on the content wrapper in BlogLayout.astro (`<div class="prose prose-lg max-w-none">`).
  </action>
  <verify>
    <automated>cd D:/work/blog && test -f src/components/mdx/Img.astro && test -f src/components/mdx/Link.astro && test -f src/components/mdx/Table.astro && test -f src/components/mdx/Blockquote.astro && grep -q "components=" src/pages/blogs/\\[slug\\].astro && grep -q "pre: CodeBlock" src/pages/blogs/\\[slug\\].astro && grep -q "img: Img" src/pages/blogs/\\[slug\\].astro && grep -q "a: Link" src/pages/blogs/\\[slug\\].astro && grep -q "table: Table" src/pages/blogs/\\[slug\\].astro && grep -q "blockquote: Blockquote" src/pages/blogs/\\[slug\\].astro && grep -q "export function Img" src/components/ui/img.tsx && grep -q ".prose p" src/styles/globals.css && grep -q ".prose thead" src/styles/globals.css && npx astro build 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/mdx/Img.astro exists with `loading="lazy"` and `max-w-full`
    - src/components/mdx/Link.astro exists with `target` and `text-primary underline`
    - src/components/mdx/Table.astro exists with `overflow-x-auto` wrapper and `border-collapse`
    - src/components/mdx/Blockquote.astro exists with `border-l-2 border-primary` and `italic`
    - src/components/ui/img.tsx is unchanged (still exports `function Img`)
    - src/pages/blogs/[slug].astro contains `components=` on Content element
    - src/pages/blogs/[slug].astro maps h2, h3, h4, h5, h6, pre, img, a, table, blockquote (10 entries)
    - src/pages/blogs/[slug].astro does NOT contain `h1:` or `code:` in mdxComponents
    - src/styles/globals.css contains `.prose p`, `.prose ul`, `.prose thead`, `.prose th`, `.prose td`
    - `npx astro build` completes without errors (all 9 posts render)
  </acceptance_criteria>
  <done>All 4 simple MDX components created, all components wired into [slug].astro via components prop, prose typography and table CSS added to globals.css, astro build succeeds with all 9 posts</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify MDX rendering in browser</name>
  <files>none</files>
  <read_first>none</read_first>
  <action>Present verification steps to user and await approval.</action>
  <what-built>Complete MDX component layer: heading anchors, syntax-highlighted code blocks with copy button, images, links, tables, blockquotes, and prose typography -- all wired into blog post pages</what-built>
  <how-to-verify>
    1. Run `npx astro dev` (should start at localhost:4321)
    2. Navigate to a blog post with code blocks (e.g., /blogs/xp-precompilation or any post with code)
    3. Verify: Code blocks have syntax highlighting with colors (not monochrome plain text)
    4. Verify: Hover over a code block -- copy button appears in top-right corner
    5. Verify: Click the copy button -- icon briefly changes to checkmark, code is in clipboard
    6. Verify: Hover over an h2 or h3 heading -- anchor link icon appears to the right
    7. Verify: Click the anchor icon -- URL hash updates and URL is copied to clipboard
    8. Navigate to /blogs/next-self-hosted (post 9 with explicit Img import)
    9. Verify: Images render without broken image errors
    10. Verify: Links in posts have underline styling
    11. (Optional) Toggle dark mode by adding class="dark" to html element in DevTools: code blocks should switch to dark theme colors
  </how-to-verify>
  <verify>Human confirms all verification steps pass</verify>
  <acceptance_criteria>
    - User confirms code blocks have syntax highlighting
    - User confirms copy button works on code blocks
    - User confirms heading anchor links appear on hover
    - User confirms post 9 images render correctly
    - User confirms link styling is correct
  </acceptance_criteria>
  <done>User approves visual and functional verification of all MDX components</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx astro build` completes without errors
- All 11 component files exist in src/components/mdx/ (H2-H6, CodeBlock, InlineCode, Img, Link, Table, Blockquote)
- src/pages/blogs/[slug].astro passes components prop to Content
- src/styles/globals.css has Shiki dark theme, inline code, table, and prose typography CSS
- Blog posts render code blocks, headings with anchors, images, links, and tables correctly
</verification>

<success_criteria>
- All MDX components render without import errors across all 9 blog posts
- Code blocks display Shiki syntax highlighting (build-time, zero JS for highlighting)
- Copy button on code blocks works (copies code to clipboard)
- Heading anchors work (hover-reveal, copy URL, navigate to fragment)
- Images render with lazy loading
- External links open in new tab
- Tables have overflow scrolling
- Blockquotes have left border accent
- Post 9's explicit Img JSX import continues to work
- `npx astro build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-mdx-components/03-02-SUMMARY.md`
</output>
