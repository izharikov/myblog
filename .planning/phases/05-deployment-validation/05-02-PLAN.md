---
phase: 05-deployment-validation
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified: []
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - PERF-01
  - PERF-02
user_setup:
  - service: cloudflare
    why: "Cloudflare Pages deployment requires authenticated wrangler CLI"
    env_vars: []
    dashboard_config:
      - task: "Authenticate wrangler CLI (browser OAuth flow)"
        location: "Run `npx wrangler login` — opens browser for Cloudflare account login"
must_haves:
  truths:
    - "Site is deployed and accessible at blog-astro-preview.pages.dev"
    - "All 12 page URLs return HTTP 200 on the deployed site"
    - "llms.txt is accessible on the deployed site and lists 9 blog posts"
    - "Images load correctly on the deployed site (no 404s on hero avatar or blog card logos)"
    - "Raw markdown access works at /blogs/{slug}.md for posts with matching filenames"
    - "Total JS bundle is smaller than 50KB (Next.js baseline is 100-300KB)"
    - "Lighthouse performance score is >= 95 on a blog detail page"
  artifacts:
    - path: "dist/"
      provides: "Production build output"
      contains: "index.html"
    - path: "dist/_astro/"
      provides: "Hashed JS/CSS assets"
      contains: "web.*.js"
  key_links:
    - from: "wrangler pages deploy"
      to: "dist/"
      via: "pages_build_output_dir in wrangler.jsonc"
      pattern: "pages_build_output_dir.*dist"
    - from: "blog-astro-preview.pages.dev"
      to: "dist/index.html"
      via: "Cloudflare Pages hosting"
      pattern: "HTTP 200"
---

<objective>
Deploy the Astro site to a preview Cloudflare Pages project and validate all routes, bundle size, and Lighthouse performance.

Purpose: This is the final validation — proving the migration works in production. The site must be accessible, all routes must return 200, JS bundle must be smaller than Next.js, and Lighthouse must score >= 95.

Output: Deployed site at blog-astro-preview.pages.dev, verified route list, bundle size measurements, Lighthouse score.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deployment-validation/05-RESEARCH.md
@.planning/phases/05-deployment-validation/05-01-SUMMARY.md

@wrangler.jsonc
@package.json
@astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build, measure bundle size, and deploy to Cloudflare Pages preview</name>
  <files>none (measurement and deploy only; astro.config.mjs is read but NOT modified)</files>
  <read_first>
    - package.json (confirm deploy:preview script exists from Plan 01)
    - wrangler.jsonc (confirm pages_build_output_dir: ./dist)
    - astro.config.mjs (current config — read only, do NOT modify)
  </read_first>
  <action>
    **Step 1: Install bundle visualizer (dev dependency)**
    ```bash
    npm install rollup-plugin-visualizer --save-dev --legacy-peer-deps
    ```

    **Step 2: Temporarily add visualizer to astro.config.mjs**
    Add `import { visualizer } from 'rollup-plugin-visualizer';` at top.
    Add `visualizer({ emitFile: true, filename: 'stats.html' })` to the vite.plugins array after `tailwindcss()`.

    **Step 3: Build and measure**
    ```bash
    npm run build
    ```
    Then measure total JS size:
    ```bash
    ls -la dist/_astro/*.js
    ```
    Expected: ~22KB total (web.js 13KB + client.js 5.6KB + MobileMenu.js 2.2KB + ThemeToggle.js 1.4KB). This is far below Next.js baseline of 100-300KB.

    Record the exact total JS size for the summary.

    **Step 4: Remove visualizer from astro.config.mjs**
    Remove the `import { visualizer }` line and the `visualizer(...)` call from vite.plugins. The file must return to its exact pre-modification state. The stats.html in dist/ will be overwritten on next build.

    **Step 5: Verify astro.config.mjs is clean**
    ```bash
    grep -c "visualizer" astro.config.mjs
    ```
    Must return 0. If not, re-edit until clean.

    **Step 6: Verify wrangler auth**
    ```bash
    npx wrangler whoami
    ```
    If not authenticated, the executor should note this requires `npx wrangler login` (human action — wrangler opens a browser OAuth flow).

    **Step 7: Create preview project (if it does not exist)**
    ```bash
    npx wrangler pages project create blog-astro-preview --production-branch main
    ```
    If the project already exists, this will error — that is fine, continue.

    **Step 8: Deploy**
    ```bash
    npx wrangler pages deploy dist/ --project-name blog-astro-preview
    ```
    Record the deployed URL from wrangler output. Note: first-time deploys receive a hash-prefixed URL (e.g., `https://abc123.blog-astro-preview.pages.dev`). The canonical `blog-astro-preview.pages.dev` alias may not be available immediately. Use whichever URL wrangler outputs for subsequent verification.
  </action>
  <verify>
    <automated>npx wrangler pages deployment list --project-name blog-astro-preview 2>&1 | head -10 && echo "--- visualizer check ---" && grep -c "visualizer" astro.config.mjs; test $? -eq 1 && echo "astro.config.mjs clean: OK"</automated>
  </verify>
  <acceptance_criteria>
    - `wrangler pages deployment list --project-name blog-astro-preview` shows at least 1 deployment
    - Total JS files in dist/_astro/*.js sum to less than 50KB (Next.js baseline 100-300KB)
    - astro.config.mjs does NOT contain "visualizer" after task completes (grep returns 0 matches)
  </acceptance_criteria>
  <done>Site deployed to blog-astro-preview.pages.dev; JS bundle confirmed smaller than Next.js; astro.config.mjs unchanged from pre-task state</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify deployed site — all routes, images, raw markdown, and Lighthouse</name>
  <files>none (verification only)</files>
  <action>
    Before presenting the checkpoint to the user, the executor should automatically curl-test all routes on the deployed URL (from Task 1 output) and report results.

    **Automated route check (executor runs before checkpoint):**
    Use the deployed URL from Task 1 output (may be hash-prefixed on first deploy). Curl each path and confirm HTTP 200:

    Page routes:
    - / (home)
    - /about
    - /blogs
    - /blogs/xp-precompilation
    - /blogs/send-mcp
    - /blogs/sitecore-ci-cd
    - /blogs/sitecore-ch-dam-exam
    - /blogs/sitecoreai-mcp-marketplace-symposium2025-recap
    - /blogs/sitecore-mcp-server-issue
    - /blogs/sitecore-marketplace-updates
    - /blogs/sitecore-marketplace-ai-tools
    - /blogs/sitecore-marketplace-app-server-side-authentication

    SEO/meta routes:
    - /llms.txt
    - /sitemap-index.xml
    - /robots.txt

    Raw markdown access (locked user constraint):
    - /blogs/xp-precompilation.md

    Image checks (DEPLOY-03 — image optimization works in production):
    - Curl the home page, extract the hero avatar image src, confirm it returns HTTP 200
    - Curl /blogs, extract at least one blog card logo image src, confirm it returns HTTP 200

    Report the HTTP status code for each URL. Then present the checkpoint to the user.
  </action>
  <what-built>
    The Astro blog has been deployed to a preview Cloudflare Pages project. All pages, llms.txt, sitemap, raw markdown files, and images should be accessible at the deployed URL.
  </what-built>
  <how-to-verify>
    The executor will have printed the deployed URL and the route check results including image and raw markdown checks.

    **1. Visual verification (human):**
    - Open the deployed URL in a browser
    - Confirm home page renders with hero section and 3 latest posts
    - Confirm the hero avatar image loads (not broken)
    - Navigate to /blogs — confirm grid of cards appears with logo images visible
    - Click a blog post — confirm MDX content renders with syntax highlighting
    - Toggle dark mode — confirm it persists after reload
    - Test mobile menu on narrow viewport
    - Check /llms.txt — should list 9 blog posts as plain text

    **2. Lighthouse (human):**
    - Go to https://pagespeed.web.dev/
    - Enter the deployed blog detail URL (e.g., https://{deployed-host}/blogs/xp-precompilation)
    - Run analysis — confirm Performance score >= 95
    - Note the score for the record
  </how-to-verify>
  <verify>
    <automated>echo "Automated route checks were performed inline before this checkpoint. Results printed above. The canonical alias blog-astro-preview.pages.dev may not resolve immediately on first deploy — use the hash-prefixed URL from Task 1 wrangler output."</automated>
  </verify>
  <acceptance_criteria>
    - All 15 page/SEO URLs return HTTP 200
    - /blogs/xp-precompilation.md returns HTTP 200 (raw markdown access — locked user constraint)
    - /llms.txt response contains "# Igor Zharikov Blog" and 9 blog links
    - /robots.txt response contains "sitemap-index.xml"
    - /sitemap-index.xml response contains valid XML
    - Hero section avatar image returns HTTP 200 (DEPLOY-03 image verification)
    - At least one blog card logo image renders without 404 on /blogs (DEPLOY-03 image verification)
    - Human confirms visual correctness
    - Lighthouse Performance score >= 95
  </acceptance_criteria>
  <done>All routes verified including images and raw markdown; visual inspection passed; Lighthouse >= 95 confirmed</done>
  <resume-signal>Type "approved" with the Lighthouse score, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Site deployed and accessible at blog-astro-preview.pages.dev URL
2. All 12 page routes + llms.txt + sitemap + robots.txt return HTTP 200
3. /blogs/xp-precompilation.md returns HTTP 200 (raw markdown access)
4. Hero avatar and blog card logo images load without 404 (DEPLOY-03)
5. Total JS bundle < 50KB (vs Next.js 100-300KB baseline)
6. Lighthouse Performance >= 95 on blog detail page
7. Visual inspection confirms full functionality (dark mode, mobile menu, syntax highlighting)
</verification>

<success_criteria>
- Deployed site is live and all routes work (including raw markdown and images)
- Bundle size is measurably smaller than Next.js
- Lighthouse score meets the >= 95 target
- Human has visually verified the site works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-deployment-validation/05-02-SUMMARY.md`
</output>
