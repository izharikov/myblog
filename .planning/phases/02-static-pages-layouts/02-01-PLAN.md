---
phase: 02-static-pages-layouts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/globals.css
  - src/lib/date.ts
  - src/lib/json-ld.ts
  - src/layouts/BaseLayout.astro
  - src/layouts/BlogLayout.astro
autonomous: true
requirements: [CONT-06, SEO-01, SEO-02]

must_haves:
  truths:
    - "Tailwind design tokens (text-primary, bg-card, bg-background, text-muted-foreground, bg-secondary) render visible color in the browser"
    - "parseDate('20.01.2025') returns a Date object for Jan 20 2025"
    - "formatDate(new Date(2025, 0, 20)) returns 'Jan 20, 2025'"
    - "BaseLayout renders a full HTML page with <head> containing title, description, canonical URL, and OG tags"
    - "BlogLayout extends BaseLayout and adds JSON-LD BlogPosting script tag"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "Tailwind v4 @theme inline block + :root + .dark CSS variable definitions"
      contains: "@theme inline"
    - path: "src/lib/date.ts"
      provides: "parseDate and formatDate utility functions"
      exports: ["parseDate", "formatDate"]
    - path: "src/layouts/BaseLayout.astro"
      provides: "HTML shell with head metadata, header slot, footer slot, CSS import"
      contains: "og:title"
    - path: "src/layouts/BlogLayout.astro"
      provides: "Blog post layout with JSON-LD structured data"
      contains: "application/ld+json"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/globals.css"
      via: "import statement"
      pattern: "import.*globals.css"
    - from: "src/layouts/BlogLayout.astro"
      to: "src/lib/json-ld.ts"
      via: "generateBlogJsonLd call"
      pattern: "generateBlogJsonLd"
    - from: "src/layouts/BlogLayout.astro"
      to: "src/lib/date.ts"
      via: "parseDate import"
      pattern: "import.*parseDate.*from.*date"
---

<objective>
Port Tailwind v4 CSS design tokens, create date utility functions, and build BaseLayout + BlogLayout — the foundation all pages depend on.

Purpose: Every page and component in Phase 2 depends on CSS tokens for color and layouts for HTML structure. This must be done first.
Output: globals.css with full token definitions, date.ts utility, BaseLayout.astro with SEO head, BlogLayout.astro with JSON-LD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-pages-layouts/02-RESEARCH.md
@src/config/site.ts
@src/content.config.ts
@src/styles/globals.css
@src/lib/json-ld.ts
@src/lib/blogs.ts
@src/types/blog.ts
@astro.config.mjs

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/content.config.ts — Zod schema (what getCollection('blog') entries contain):
```typescript
schema: z.object({
  title: z.string(),
  slug: z.string().optional(),
  logo: z.string(),
  description: z.string(),
  tags: z.array(z.string()),
  date: z.string(),  // Format: "DD.MM.YYYY"
})
```

From src/config/site.ts:
```typescript
export const siteConfig = {
  name: "Igor Zharikov Blog",
  logo: "@izharikov",
  description: `I'm Sitecore Architect at Brimit. Sitecore MVP 2023-2026`,
  site: "https://izharikov.dev",
  author: { name: "Igor Zharikov", email: "zharikovigor97@gmail.com" },
  social: {
    github: "https://github.com/izharikov",
    linkedin: "https://www.linkedin.com/in/ihar-zharykau/",
    twitter: "https://x.com/i_zharikov",
  },
  navigation: [
    { name: "Home", href: "/" },
    { name: "About Me", href: "/about" },
    { name: "Blogs", href: "/blogs" },
  ],
};
```

From src/types/blog.ts:
```typescript
export type BlogPost = {
  path: string; slug: string; title: string; description: string;
  date: Date; dateDisplay: string; logo: string; tags: string[];
}
export type BlogMeta = Omit<BlogPost, 'dateDisplay' | 'date'> & { date: string; };
```

From src/lib/json-ld.ts (existing, needs schema-dts installed):
```typescript
import { BlogPosting, Person, WithContext } from 'schema-dts';
export function personJsonLd(): WithContext<Person>;
export function generateBlogJsonLd(post: BlogPost): WithContext<BlogPosting>;
// post.date must be a Date object (calls .toISOString())
// post.slug used for mainEntityOfPage URL
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port CSS design tokens and create date utilities</name>
  <files>src/styles/globals.css, src/lib/date.ts</files>
  <read_first>
    - src/styles/globals.css (current state — only has @import "tailwindcss" and font imports)
    - src/lib/blogs.ts (contains original parseDate/formatDate to port)
  </read_first>
  <action>
**1. Update `src/styles/globals.css`** — Add the following blocks AFTER the existing font imports (keep the existing @import "tailwindcss", @import font lines, and :root font-family block):

Add `@custom-variant dark (&:is(.dark *));` after the @import lines.

Add the full `@theme inline` block:
```css
@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
  --color-sidebar-ring: var(--sidebar-ring);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar: var(--sidebar);
  --color-chart-5: var(--chart-5);
  --color-chart-4: var(--chart-4);
  --color-chart-3: var(--chart-3);
  --color-chart-2: var(--chart-2);
  --color-chart-1: var(--chart-1);
  --color-ring: var(--ring);
  --color-input: var(--input);
  --color-border: var(--border);
  --color-destructive: var(--destructive);
  --color-accent-foreground: var(--accent-foreground);
  --color-accent: var(--accent);
  --color-muted-foreground: var(--muted-foreground);
  --color-muted: var(--muted);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-secondary: var(--secondary);
  --color-primary-foreground: var(--primary-foreground);
  --color-primary: var(--primary);
  --color-popover-foreground: var(--popover-foreground);
  --color-popover: var(--popover);
  --color-card-foreground: var(--card-foreground);
  --color-card: var(--card);
  --radius-sm: calc(var(--radius) - 4px);
  --radius-md: calc(var(--radius) - 2px);
  --radius-lg: var(--radius);
  --radius-xl: calc(var(--radius) + 4px);
}
```

Replace the existing `:root` block (which only has font vars) with the full `:root` block containing all OKLCH color values:
```css
:root {
  --font-geist-sans: 'Geist Variable', sans-serif;
  --font-geist-mono: 'Geist Mono Variable', monospace;
  --radius: 0.625rem;
  --background: oklch(1 0 0);
  --foreground: oklch(0.129 0.042 264.695);
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.129 0.042 264.695);
  --popover: oklch(1 0 0);
  --popover-foreground: oklch(0.129 0.042 264.695);
  --primary: oklch(0.208 0.042 265.755);
  --primary-foreground: oklch(0.984 0.003 247.858);
  --secondary: oklch(0.968 0.007 247.896);
  --secondary-foreground: oklch(0.208 0.042 265.755);
  --muted: oklch(0.968 0.007 247.896);
  --muted-foreground: oklch(0.554 0.046 257.417);
  --accent: oklch(0.968 0.007 247.896);
  --accent-foreground: oklch(0.208 0.042 265.755);
  --destructive: oklch(0.577 0.245 27.325);
  --border: oklch(0.929 0.013 255.508);
  --input: oklch(0.929 0.013 255.508);
  --ring: oklch(0.704 0.04 256.788);
  --chart-1: oklch(0.646 0.222 41.116);
  --chart-2: oklch(0.6 0.118 184.704);
  --chart-3: oklch(0.398 0.07 227.392);
  --chart-4: oklch(0.828 0.189 84.429);
  --chart-5: oklch(0.769 0.188 70.08);
  --sidebar: oklch(0.984 0.003 247.858);
  --sidebar-foreground: oklch(0.129 0.042 264.695);
  --sidebar-primary: oklch(0.208 0.042 265.755);
  --sidebar-primary-foreground: oklch(0.984 0.003 247.858);
  --sidebar-accent: oklch(0.968 0.007 247.896);
  --sidebar-accent-foreground: oklch(0.208 0.042 265.755);
  --sidebar-border: oklch(0.929 0.013 255.508);
  --sidebar-ring: oklch(0.704 0.04 256.788);
}
```

Add the `.dark` block with all dark mode OKLCH values:
```css
.dark {
  --background: oklch(0.129 0.042 264.695);
  --foreground: oklch(0.984 0.003 247.858);
  --card: oklch(0.208 0.042 265.755);
  --card-foreground: oklch(0.984 0.003 247.858);
  --popover: oklch(0.208 0.042 265.755);
  --popover-foreground: oklch(0.984 0.003 247.858);
  --primary: oklch(0.929 0.013 255.508);
  --primary-foreground: oklch(0.208 0.042 265.755);
  --secondary: oklch(0.279 0.041 260.031);
  --secondary-foreground: oklch(0.984 0.003 247.858);
  --muted: oklch(0.279 0.041 260.031);
  --muted-foreground: oklch(0.704 0.04 256.788);
  --accent: oklch(0.279 0.041 260.031);
  --accent-foreground: oklch(0.984 0.003 247.858);
  --destructive: oklch(0.704 0.191 22.216);
  --border: oklch(1 0 0 / 10%);
  --input: oklch(1 0 0 / 15%);
  --ring: oklch(0.551 0.027 264.364);
  --chart-1: oklch(0.488 0.243 264.376);
  --chart-2: oklch(0.696 0.17 162.48);
  --chart-3: oklch(0.769 0.188 70.08);
  --chart-4: oklch(0.627 0.265 303.9);
  --chart-5: oklch(0.645 0.246 16.439);
  --sidebar: oklch(0.208 0.042 265.755);
  --sidebar-foreground: oklch(0.984 0.003 247.858);
  --sidebar-primary: oklch(0.488 0.243 264.376);
  --sidebar-primary-foreground: oklch(0.984 0.003 247.858);
  --sidebar-accent: oklch(0.279 0.041 260.031);
  --sidebar-accent-foreground: oklch(0.984 0.003 247.858);
  --sidebar-border: oklch(1 0 0 / 10%);
  --sidebar-ring: oklch(0.551 0.027 264.364);
}
```

Add the `@layer base` block:
```css
@layer base {
  * {
    @apply border-border outline-ring/50;
  }
  body {
    @apply bg-background text-foreground;
  }
}
```

Do NOT add `@import "tw-animate-css"` (not installed, not needed for Phase 2).

**2. Create `src/lib/date.ts`** — New file with these exact exports:
```typescript
/**
 * Parse DD.MM.YYYY date string to Date object.
 * Frontmatter dates use this format (e.g., "20.01.2025").
 */
export function parseDate(dateString: string): Date {
  const [day, month, year] = dateString.split('.');
  return new Date(parseInt(year), parseInt(month) - 1, parseInt(day));
}

/**
 * Format a Date object for display (en-US locale, short month).
 * Output: "Jan 20, 2025"
 */
export function formatDate(date: Date): string {
  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'short',
    day: 'numeric',
  });
}
```
  </action>
  <verify>
    <automated>cd D:/work/blog && grep -c "@theme inline" src/styles/globals.css && grep -c "oklch" src/styles/globals.css && grep -c "export function parseDate" src/lib/date.ts && grep -c "export function formatDate" src/lib/date.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/styles/globals.css contains `@theme inline {` block
    - src/styles/globals.css contains `@custom-variant dark (&:is(.dark *));`
    - src/styles/globals.css contains `:root {` with `--background: oklch(1 0 0);`
    - src/styles/globals.css contains `.dark {` with `--background: oklch(0.129 0.042 264.695);`
    - src/styles/globals.css contains `@layer base` with `@apply bg-background text-foreground`
    - src/styles/globals.css still contains `@import "tailwindcss";`
    - src/styles/globals.css still contains `@import "@fontsource-variable/geist/wght.css";`
    - src/lib/date.ts contains `export function parseDate(dateString: string): Date`
    - src/lib/date.ts contains `export function formatDate(date: Date): string`
    - src/lib/date.ts contains `toLocaleDateString('en-US'`
  </acceptance_criteria>
  <done>globals.css has all Tailwind v4 design tokens ported from original; date.ts exports parseDate and formatDate functions matching original behavior</done>
</task>

<task type="auto">
  <name>Task 2: Install schema-dts and create BaseLayout + BlogLayout</name>
  <files>src/layouts/BaseLayout.astro, src/layouts/BlogLayout.astro, src/lib/json-ld.ts</files>
  <read_first>
    - src/styles/globals.css (just updated with CSS tokens — verify import path)
    - src/config/site.ts (siteConfig used for SEO metadata)
    - src/lib/json-ld.ts (existing file — needs schema-dts installed; uses BlogPost type)
    - src/types/blog.ts (BlogPost type shape — json-ld.ts imports this)
    - src/lib/date.ts (just created — parseDate/formatDate for BlogLayout)
  </read_first>
  <action>
**1. Install schema-dts:**
```bash
npm install schema-dts
```

**2. Create `src/layouts/BaseLayout.astro`:**
```astro
---
import '@/styles/globals.css';
import { siteConfig } from '@/config/site';

interface Props {
  title: string;
  description: string;
  canonicalUrl?: string;
  ogImage?: string;
  ogType?: 'website' | 'article';
}

const { title, description, canonicalUrl, ogImage, ogType = 'website' } = Astro.props;
const canonical = canonicalUrl ?? new URL(Astro.url.pathname, siteConfig.site).href;
const fullOgImage = ogImage ? new URL(ogImage, siteConfig.site).href : undefined;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:type" content={ogType} />
    {fullOgImage && <meta property="og:image" content={fullOgImage} />}
    <meta name="generator" content={Astro.generator} />
  </head>
  <body class="min-h-screen bg-background text-foreground font-sans antialiased">
    <slot name="header" />
    <main>
      <slot />
    </main>
    <slot name="footer" />
  </body>
</html>
```

NOTE: Header and Footer are NOT imported here — they will be placed into named slots by page files (or alternatively, import them directly). The simpler approach: import Header and Footer directly in BaseLayout once they exist in Plan 02. For now, use named slots so Plan 01 can be verified independently. Plan 02 will update BaseLayout to import Header/Footer directly.

Actually, to keep it simple and avoid a back-and-forth update: import Header and Footer placeholders. Since Header.astro and Footer.astro don't exist yet, instead render the `<slot />` content between a simple structure. Pages will import the layout and get the HTML shell. Plan 02 will update BaseLayout to add Header/Footer imports once those components exist.

Revised approach: Do NOT use named slots. Just use a single default `<slot />`. The body should be:
```html
<body class="min-h-screen bg-background text-foreground font-sans antialiased">
  <main>
    <slot />
  </main>
</body>
```

Plan 02 will add Header/Footer imports directly into BaseLayout once those components are created.

**3. Create `src/layouts/BlogLayout.astro`:**
```astro
---
import BaseLayout from './BaseLayout.astro';
import { generateBlogJsonLd } from '@/lib/json-ld';
import { parseDate, formatDate } from '@/lib/date';
import { siteConfig } from '@/config/site';

interface Props {
  title: string;
  description: string;
  date: string;       // DD.MM.YYYY from frontmatter
  slug: string;       // post.id from collection
  logo: string;
  tags: string[];
}

const { title, description, date, slug, logo, tags } = Astro.props;
const parsedDate = parseDate(date);
const dateDisplay = formatDate(parsedDate);
const ogImage = new URL(logo, siteConfig.site).href;

const jsonLd = generateBlogJsonLd({
  title,
  description,
  date: parsedDate,
  dateDisplay,
  slug,
  logo,
  tags,
  path: slug,
});
---
<BaseLayout title={title} description={description} ogImage={ogImage} ogType="article">
  <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
  <article class="container max-w-screen-xl mx-auto px-4 py-8">
    <header class="mb-8 space-y-4">
      <h1 class="text-3xl font-bold tracking-tight sm:text-4xl">{title}</h1>
      <div class="flex items-center gap-4 text-sm text-muted-foreground">
        <time datetime={parsedDate.toISOString().split('T')[0]}>{dateDisplay}</time>
      </div>
      <div class="flex gap-2 flex-wrap">
        {tags.map(tag => (
          <span class="inline-flex items-center rounded-full border border-transparent bg-secondary text-secondary-foreground px-2 py-0.5 text-xs font-medium">
            {tag}
          </span>
        ))}
      </div>
    </header>
    <div class="prose prose-lg max-w-none">
      <slot />
    </div>
  </article>
</BaseLayout>
```

**4. Update `src/lib/json-ld.ts`** — The existing file imports `BlogPost` from `@/types/blog`. The `BlogPost` type requires `path`, `slug`, `title`, `description`, `date` (Date), `dateDisplay`, `logo`, `tags`. The `generateBlogJsonLd` function uses `post.date.toISOString()`, `post.slug`, `post.title`, `post.description`, `post.logo`. Keep the file as-is — it will work once `schema-dts` is installed. No changes needed.
  </action>
  <verify>
    <automated>cd D:/work/blog && npx astro check 2>&1 | tail -5 && grep -c "og:title" src/layouts/BaseLayout.astro && grep -c "application/ld+json" src/layouts/BlogLayout.astro && node -e "require('schema-dts')" 2>&1 | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls schema-dts` shows schema-dts installed
    - src/layouts/BaseLayout.astro contains `import '@/styles/globals.css'`
    - src/layouts/BaseLayout.astro contains `<meta property="og:title"`
    - src/layouts/BaseLayout.astro contains `<meta property="og:description"`
    - src/layouts/BaseLayout.astro contains `<meta property="og:type"`
    - src/layouts/BaseLayout.astro contains `<link rel="canonical"`
    - src/layouts/BaseLayout.astro contains `<meta name="description"`
    - src/layouts/BlogLayout.astro contains `import BaseLayout from './BaseLayout.astro'`
    - src/layouts/BlogLayout.astro contains `application/ld+json`
    - src/layouts/BlogLayout.astro contains `generateBlogJsonLd`
    - src/layouts/BlogLayout.astro contains `parseDate`
    - `npx astro check` exits with 0 errors (warnings acceptable)
  </acceptance_criteria>
  <done>BaseLayout renders full HTML page with SEO metadata (title, description, canonical, OG tags) per SEO-01 and SEO-02; BlogLayout extends BaseLayout with JSON-LD BlogPosting schema and article markup; schema-dts installed for json-ld.ts type safety</done>
</task>

</tasks>

<verification>
- `npx astro check` passes (0 errors)
- `grep "@theme inline" src/styles/globals.css` returns a match
- `grep "og:title" src/layouts/BaseLayout.astro` returns a match
- `grep "application/ld+json" src/layouts/BlogLayout.astro` returns a match
- src/lib/date.ts exports parseDate and formatDate
</verification>

<success_criteria>
1. globals.css contains all Tailwind v4 design tokens (light + dark OKLCH values)
2. date.ts exports parseDate (DD.MM.YYYY -> Date) and formatDate (Date -> "Jan 20, 2025")
3. BaseLayout.astro renders complete HTML with SEO head tags
4. BlogLayout.astro extends BaseLayout with JSON-LD and article structure
5. schema-dts is installed
6. `npx astro check` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-pages-layouts/02-01-SUMMARY.md`
</output>
