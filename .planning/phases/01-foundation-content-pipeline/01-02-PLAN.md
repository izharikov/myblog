---
phase: 01-foundation-content-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - src/styles/globals.css
  - src/pages/index.astro
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-05

must_haves:
  truths:
    - "npx astro dev starts without errors and serves a response at localhost:4321"
    - "No Next.js or React packages remain in package.json dependencies"
    - "Tailwind utility classes are processed (not zero styles) — the astro.config.mjs uses @tailwindcss/vite in vite.plugins, not integrations"
    - "SolidJS integration is enabled — @astrojs/solid-js appears in integrations array in astro.config.mjs"
    - "TypeScript path alias @/* resolves to ./src/* in tsconfig.json"
    - "astro check completes without TypeScript errors on the scaffold files"
  artifacts:
    - path: "package.json"
      provides: "Astro project manifest with all Phase 1 dependencies"
      contains: "\"astro\""
    - path: "astro.config.mjs"
      provides: "Astro configuration with Tailwind v4 Vite plugin, SolidJS, MDX, sitemap integrations"
      contains: "@tailwindcss/vite"
    - path: "tsconfig.json"
      provides: "Astro-compatible TypeScript config with SolidJS JSX and path aliases"
      contains: "astro/tsconfigs/strict"
    - path: "src/styles/globals.css"
      provides: "Tailwind v4 CSS entry point with font imports and CSS variables"
      contains: "@import \"tailwindcss\""
    - path: "src/pages/index.astro"
      provides: "Placeholder page enabling astro dev to start"
      contains: "Phase 1 scaffold"
  key_links:
    - from: "astro.config.mjs"
      to: "@tailwindcss/vite"
      via: "vite.plugins array"
      pattern: "plugins.*tailwindcss"
    - from: "astro.config.mjs"
      to: "@astrojs/solid-js"
      via: "integrations array"
      pattern: "solidJs()"
    - from: "src/styles/globals.css"
      to: "@fontsource-variable/geist"
      via: "@import directive"
      pattern: "@import.*geist"
    - from: "tsconfig.json"
      to: "src/"
      via: "paths alias @/*"
      pattern: "\"@/\\*\""
---

<objective>
Delete all Next.js/React files and install the complete Astro 6 scaffold: package.json, astro.config.mjs, tsconfig.json, globals.css, and a placeholder index page.

Purpose: This plan transforms the repository from a Next.js project into an Astro project. It is the structural prerequisite for Plan 03 (content collections). After this plan completes, `npx astro dev` must start without errors.

Output: Working Astro 6 project with Tailwind v4, SolidJS integration, MDX integration, and a placeholder homepage. The Next.js project files are gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-content-pipeline/01-RESEARCH.md

<interfaces>
<!-- Verified config patterns from 01-RESEARCH.md — use these exactly -->

astro.config.mjs canonical pattern:
```javascript
import { defineConfig } from 'astro/config';
import solidJs from '@astrojs/solid-js';
import mdx from '@astrojs/mdx';
import sitemap from '@astrojs/sitemap';
import cloudflare from '@astrojs/cloudflare';
import tailwindcss from '@tailwindcss/vite';
import remarkGfm from 'remark-gfm';

export default defineConfig({
  site: 'https://izharikov.dev',
  output: 'static',
  adapter: cloudflare(),
  integrations: [
    solidJs(),
    mdx({ remarkPlugins: [remarkGfm] }),
    sitemap(),
  ],
  vite: {
    plugins: [tailwindcss()],
  },
});
```

tsconfig.json canonical pattern:
```json
{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist", "node_modules"],
  "compilerOptions": {
    "jsx": "preserve",
    "jsxImportSource": "solid-js",
    "paths": {
      "@/*": ["./src/*"],
      "@/blogs/*": ["./blogs/*"]
    }
  }
}
```

globals.css canonical pattern:
```css
@import "tailwindcss";
@import "@fontsource-variable/geist/wght.css";
@import "@fontsource-variable/geist-mono/wght.css";

:root {
  --font-geist-sans: 'Geist Variable', sans-serif;
  --font-geist-mono: 'Geist Mono Variable', monospace;
}
```

package.json scripts:
```json
{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "astro build && wrangler pages deploy dist/"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Next.js files and install Astro dependencies</name>
  <files>
    package.json
  </files>
  <read_first>
    - package.json (read current deps to know what to remove before overwriting)
  </read_first>
  <action>
Step 1 — Delete all Next.js/React project files. Run these deletions:

Files to delete at project root:
- `next.config.ts`
- `next-env.d.ts`
- `open-next.config.ts`
- `cloudflare-env.d.ts`
- `postcss.config.mjs`
- `components.json`
- `mdx-components.tsx`
- `image-loader.ts`

Directories to delete entirely:
- `src/app/` (entire Next.js App Router directory)
- `src/hooks/` (React-specific hooks)
- `src/data/` (blog-manifest.ts — replaced by content collections)
- `scripts/` (generate-blog-manifest.mjs)

Do NOT delete: `blogs/`, `public/`, `src/config/`, `src/types/`, `src/lib/`, `src/components/` (will be refactored in later phases).

Step 2 — Overwrite package.json with the Astro project manifest. Use these exact dependencies (versions verified 2026-03-27):

```json
{
  "name": "blog",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "astro build && wrangler pages deploy dist/"
  },
  "dependencies": {
    "astro": "^6.1.1",
    "@astrojs/solid-js": "^6.0.1",
    "@astrojs/mdx": "^5.0.3",
    "@astrojs/sitemap": "^3.7.2",
    "@astrojs/cloudflare": "^13.1.4",
    "solid-js": "^1.9.12",
    "tailwindcss": "^4.2.2",
    "@tailwindcss/vite": "^4.2.2",
    "@fontsource-variable/geist": "^5.2.8",
    "@fontsource-variable/geist-mono": "^5.2.7",
    "remark-gfm": "^4.0.1",
    "clsx": "^2.0.0",
    "tailwind-merge": "^3.0.0",
    "lucide-solid": "^0.577.0"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "wrangler": "^4.0.0"
  }
}
```

Step 3 — Run `npm install` to install all dependencies and regenerate package-lock.json.
  </action>
  <verify>
    <automated>
      node -e "const p = JSON.parse(require('fs').readFileSync('D:/work/blog/package.json', 'utf8')); console.log('astro:', p.dependencies.astro, 'solid-js:', p.dependencies['solid-js'])"
      ls D:/work/blog/next.config.ts 2>/dev/null && echo "FAIL: next.config.ts still exists" || echo "OK: next.config.ts deleted"
      ls D:/work/blog/src/app 2>/dev/null && echo "FAIL: src/app/ still exists" || echo "OK: src/app/ deleted"
      ls D:/work/blog/node_modules/astro/package.json 2>/dev/null | head -1 && echo "OK: astro installed" || echo "FAIL: astro not installed"
    </automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"astro": "^6.1.1"` in dependencies
    - `package.json` does NOT contain `"next"` in any dependencies field
    - `package.json` does NOT contain `"react"` in any dependencies field
    - `node_modules/astro/` directory exists (npm install succeeded)
    - `next.config.ts` does not exist
    - `src/app/` directory does not exist
    - `src/data/` directory does not exist
    - `scripts/` directory does not exist
  </acceptance_criteria>
  <done>Next.js files deleted, Astro dependencies installed, package.json reflects Astro project.</done>
</task>

<task type="auto">
  <name>Task 2: Write astro.config.mjs, tsconfig.json, globals.css, and placeholder page</name>
  <files>
    astro.config.mjs
    tsconfig.json
    src/styles/globals.css
    src/pages/index.astro
  </files>
  <read_first>
    - tsconfig.json (read current state before overwriting — confirm Next.js config is present)
    - .planning/phases/01-foundation-content-pipeline/01-RESEARCH.md (verified config patterns)
  </read_first>
  <action>
Create or overwrite each file with the exact content below.

**astro.config.mjs** — create at project root:
```javascript
// astro.config.mjs
import { defineConfig } from 'astro/config';
import solidJs from '@astrojs/solid-js';
import mdx from '@astrojs/mdx';
import sitemap from '@astrojs/sitemap';
import cloudflare from '@astrojs/cloudflare';
import tailwindcss from '@tailwindcss/vite';
import remarkGfm from 'remark-gfm';

export default defineConfig({
  site: 'https://izharikov.dev',
  output: 'static',
  adapter: cloudflare(),
  integrations: [
    solidJs(),
    mdx({
      remarkPlugins: [remarkGfm],
    }),
    sitemap(),
  ],
  vite: {
    plugins: [tailwindcss()],
  },
});
```

**tsconfig.json** — overwrite at project root (replace the Next.js tsconfig entirely):
```json
{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist", "node_modules"],
  "compilerOptions": {
    "jsx": "preserve",
    "jsxImportSource": "solid-js",
    "paths": {
      "@/*": ["./src/*"],
      "@/blogs/*": ["./blogs/*"]
    }
  }
}
```

**src/styles/globals.css** — create directory if needed, write file:
```css
@import "tailwindcss";

/* Geist Variable fonts — self-hosted via Fontsource, replaces next/font/google */
@import "@fontsource-variable/geist/wght.css";
@import "@fontsource-variable/geist-mono/wght.css";

/* CSS variables for font families — replaces Next.js automatic CSS variable injection */
:root {
  --font-geist-sans: 'Geist Variable', sans-serif;
  --font-geist-mono: 'Geist Mono Variable', monospace;
}
```

**src/pages/index.astro** — create the pages directory and a minimal placeholder page:
```astro
---
// Phase 1 scaffold placeholder — replaced by real home page in Phase 2
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Blog — Phase 1 Scaffold</title>
  </head>
  <body class="bg-white text-black dark:bg-black dark:text-white p-8">
    <h1 class="text-2xl font-bold">Phase 1 scaffold complete</h1>
    <p>Astro is running. Content collections and real pages come in Phase 2.</p>
  </body>
</html>
```

Note: The `class="bg-white ... dark:bg-white"` attributes in the placeholder page are intentional — they test that Tailwind utility classes are processed by the @tailwindcss/vite plugin. If Tailwind is not configured correctly, these classes produce no styles (but no error). Verify visually if dev server is running, or check via astro check.

After writing all files, run: `npx astro check` from the project root to confirm TypeScript compiles without errors.
  </action>
  <verify>
    <automated>
      grep -q "@tailwindcss/vite" D:/work/blog/astro.config.mjs && echo "OK: Tailwind Vite plugin present" || echo "FAIL: Tailwind Vite plugin missing"
      grep -q "solidJs()" D:/work/blog/astro.config.mjs && echo "OK: SolidJS integration present" || echo "FAIL: SolidJS missing"
      grep -q "astro/tsconfigs/strict" D:/work/blog/tsconfig.json && echo "OK: Astro tsconfig extends" || echo "FAIL: wrong tsconfig base"
      grep -q '"@/\*"' D:/work/blog/tsconfig.json && echo "OK: path alias present" || echo "FAIL: path alias missing"
      grep -q '@import "tailwindcss"' D:/work/blog/src/styles/globals.css && echo "OK: Tailwind import present" || echo "FAIL: Tailwind import missing"
      grep -q "--font-geist-sans" D:/work/blog/src/styles/globals.css && echo "OK: font CSS variable defined" || echo "FAIL: font variable missing"
      ls D:/work/blog/src/pages/index.astro && echo "OK: placeholder page exists" || echo "FAIL: placeholder page missing"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "@tailwindcss/vite" astro.config.mjs` returns a match (Tailwind in vite.plugins, NOT integrations)
    - `grep "@astrojs/tailwind" astro.config.mjs` returns no match (old v3 integration NOT used)
    - `grep "solidJs()" astro.config.mjs` returns a match
    - `grep "adapter: cloudflare()" astro.config.mjs` returns a match
    - `grep "site: 'https://izharikov.dev'" astro.config.mjs` returns a match
    - `grep "astro/tsconfigs/strict" tsconfig.json` returns a match
    - `grep '"jsxImportSource": "solid-js"' tsconfig.json` returns a match
    - `grep '"@/\*"' tsconfig.json` returns a match (path alias configured)
    - `grep '@import "tailwindcss"' src/styles/globals.css` returns a match
    - `grep "@fontsource-variable/geist/wght.css" src/styles/globals.css` returns a match
    - `grep "--font-geist-sans" src/styles/globals.css` returns a match
    - `src/pages/index.astro` file exists
    - `npx astro check` exits with code 0 (no TypeScript errors on scaffold files)
  </acceptance_criteria>
  <done>astro.config.mjs, tsconfig.json, src/styles/globals.css, and src/pages/index.astro all exist with correct content. astro check passes.</done>
</task>

</tasks>

<verification>
Run the Astro dev server to confirm the scaffold works end-to-end:

```bash
cd D:/work/blog && npx astro dev
```

Expected: Server starts on port 4321 with no errors. Visiting http://localhost:4321 shows "Phase 1 scaffold complete". No `@astrojs/tailwind` errors. No TypeScript errors.

Also verify deletions are complete:
```bash
ls D:/work/blog/next.config.ts 2>/dev/null || echo "deleted"
ls D:/work/blog/src/app/ 2>/dev/null || echo "deleted"
ls D:/work/blog/cloudflare-env.d.ts 2>/dev/null || echo "deleted"
```
</verification>

<success_criteria>
- `npx astro dev` starts without errors and serves http://localhost:4321
- No Next.js or React packages in package.json dependencies
- `astro.config.mjs` uses `@tailwindcss/vite` in `vite.plugins` (not `@astrojs/tailwind` in `integrations`)
- `tsconfig.json` extends `astro/tsconfigs/strict` with `jsxImportSource: "solid-js"` and `@/*` path alias
- `src/styles/globals.css` contains `@import "tailwindcss"` and `--font-geist-sans` CSS variable
- `src/pages/index.astro` exists as a valid Astro page
- `npx astro check` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-content-pipeline/01-02-SUMMARY.md` using the summary template.
</output>
