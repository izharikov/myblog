---
import { Image } from 'astro:assets';
import { resolveImage } from '@/lib/images';

interface Props {
  title: string;
  description: string;
  slug: string;
  logo: string;
  dateDisplay: string;
  tags: string[];
  priority?: boolean;
}
const { title, description, slug, logo, dateDisplay, tags, priority } = Astro.props;
const logoImage = resolveImage(logo);
---
<a href={`/blogs/${slug}`}>
  <div class="overflow-hidden rounded-xl border bg-surface text-text-primary flex flex-col gap-6 shadow-sm transition-all duration-300 hover:shadow-xl hover:-translate-y-1 h-full">
    <div class="aspect-video border-b border-border/50 w-full bg-surface-alt flex items-center justify-center overflow-hidden">
      {logoImage ? (
        <Image src={logoImage()} alt={title} width={600} height={338} class="object-cover w-full h-full" loading={priority ? "eager" : "lazy"} fetchpriority={priority ? "high" : undefined} />
      ) : (
        <img src={logo} alt={title} width="600" height="338" class="object-cover w-full h-full" loading={priority ? "eager" : "lazy"} fetchpriority={priority ? "high" : undefined} />
      )}
    </div>
    <div class="px-6 pb-6 space-y-3">
      <h3 class="text-xl font-bold line-clamp-2">
        <span class="hover:text-accent transition-colors">{title}</span>
      </h3>
      <p class="text-sm text-text-secondary line-clamp-3">{description}</p>
      <div class="flex flex-col justify-between text-xs text-text-secondary pt-2 gap-4">
        <span>Published: {dateDisplay}</span>
        <div class="flex gap-2 flex-wrap">
          {tags.map(tag => (
            <span class="inline-flex items-center rounded-full border border-transparent bg-surface-alt text-text-primary px-2 py-0.5 text-xs font-medium">
              {tag}
            </span>
          ))}
        </div>
      </div>
    </div>
  </div>
</a>
